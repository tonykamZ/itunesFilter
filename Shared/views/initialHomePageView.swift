import SwiftUI

struct InitialHomePageView: View {
    var body: some View {
        VStack {
            Text("Start by searching for your favorite music.")
                .font(.subheadline)
                .foregroundColor(.gray)
        }
        .padding()
    }
}
